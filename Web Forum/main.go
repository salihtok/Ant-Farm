package main

/*func main() {
	// open SQLite database.
	db, err := sql.Open("sqlite3", "/home/salih/Documents/Go Projects/Web Forum/database/forum.db")
	if err != nil {
		log.Fatal(err)
	}
	defer db.Close()
	//create tables.

	err = createTables(db)
	if err != nil {
		log.Fatalf("failed to create tables: ", err)

	} else {
		log.Println("tables created successfully.")
	}

}*/
